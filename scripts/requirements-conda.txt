# requirements-conda.txt — CV Builder pip dependencies for local conda development
#
# PURPOSE: Pip packages to install AFTER conda has set up the base environment.
#          Excludes spacy, blis, and language-tool-python (managed by conda-forge
#          to avoid macOS compilation issues). Adds LLM API clients not needed in CI.
#
# EXECUTION ORDER (local conda setup):
#   1. conda create -n cvgen python=3.9 spacy -c conda-forge --override-channels -y
#   2. conda activate cvgen
#   3. pip install -r scripts/requirements-conda.txt
#
# For CI / pure-pip environments, use scripts/requirements.txt instead.

# LLM API Clients
openai>=1.0.0
anthropic>=0.18.0
sentence-transformers>=2.2.0

# Google Drive API
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0

# Document Generation
python-docx>=0.8.11
weasyprint>=60.0
pypandoc>=1.11
mammoth>=1.6.0

# Text Processing & NLP (exclude spacy/blis; installed via conda)
transformers>=4.30.0
nltk>=3.8

# BibTeX Processing
bibtexparser>=1.4.0

# Data Validation
pydantic>=2.0.0
jsonschema>=4.0.0
scikit-learn>=1.5.0

# Environment Variables
python-dotenv>=1.0.0

# Web Interface
flask>=2.3.0

# HTML/Template Processing
jinja2>=3.1.2
beautifulsoup4>=4.12.0

# Standard Libraries
requests>=2.31.0
